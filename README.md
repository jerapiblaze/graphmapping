# Graph Mapping framework

A framework for generating problems, testing and performance comparing between algorithms and approaches in solving `Graph Mapping` problem.

## Environment setup

```bash
chmod +x ./setup.sh
./setup.sh

conda activate graphmapping
```